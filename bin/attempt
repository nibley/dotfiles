#! /usr/bin/env zsh

autoload colors
colors

success="$fg_bold[green]✓$reset_color"
failure="$fg_bold[red]✗$reset_color"


if result=$("$@" 2>&1)
then
	echo "$success  \"$@\""
else
	echo "$failure  \"$@\""

	if [[ -n $result ]]
	then
		echo "     $result\n"
	fi
fi
