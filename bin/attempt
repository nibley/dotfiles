#! /usr/bin/env zsh

autoload colors
colors

success="$fg_bold[green]✓$reset_color"
failure="$fg_bold[red]✗$reset_color"

"$@" 2>/dev/null
exitcode=$?
if [[ $exitcode -eq 0 ]]
then
	echo "$success  \"$@\""
else
	echo "$failure  \"$@\""
fi
	